<h2> Introduction </h2>
<p>This poject is employed for teaching how it works a framework, so don't have great expectatives.</p>
<p>For deploying the app developed, you must use the install controller, that controller, checks if exists the file .deployed, if it doesn't exist then begins the installation process, so you will need a clean sql file, without the schema definition</p>

Finally, the process for developing is like MVC pattern. First begin implementing the model.
